version: '3.8'
services:
  mariadb:
    image: mariadb:latest
    container_name: mariadb-grades
    environment:
      MARIADB_DATABASE: gradesdb
      MARIADB_ROOT_PASSWORD: password
    ports:
      - "3306:3306"